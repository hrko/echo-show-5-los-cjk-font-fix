# 端末側フォントパッチ処理の Python 移行に向けた引き継ぎ

この資料は、2026-09-18 に作成した、Python 移行前の依頼記録です。以下の実装・作業ツリー・端末・成果物の状態は、引き継ぎ作成時点の情報です。移行後の構成は [Python ランタイムの説明](python-runtime.md)、実機での結果は [Python 版の検証記録](python-device-validation.md)を参照してください。

移行の目的は、TWRP ZIP に Python 実行環境を同梱してホストと端末で処理を共有し、TWRP ごとの shell / awk の実装差による不具合を避けることでした。Python の調達・同梱方法、処理の分担、検証方法の設計は、実装時に決める方針でした。以下に当時の要件と調査結果を記載します。

## 環境と作業ツリー

- リポジトリ: `C:\Users\haruki\Documents\repo\github.com\hrko\echo-show-5-jp-font-fix`
- ホスト: Windows / PowerShell。ビルド・テストは mise / uv / Python。
- GitHub との通信には **gh コマンド**を使ってください（作成するスクリプトは例外）。
- 引き継ぎ作成日: 2026-09-18。
- HEAD: `51d7f72a28a850100e60c4f4edb21e087cfaa7f9`。
- **独立パッチの実装と今回の実機対応は未コミットです。未追跡ファイルも多数あり、HEAD だけでは再現できません。既存の変更を保持してください。**

対象は Echo Show 5 第2世代（cronos）、LineageOS 18.1 / Android 11、TWRP `3.7.0_9-0`。ADB シリアルは `G091P308301603TS` です。対象 ROM は `lineage-18.1-20260904-UNOFFICIAL-cronos.zip`、system fingerprint は次の値です。

```text
google/lineage_cronos/cronos:11/RQ3A.211001.001/r0rt1z209050214:userdebug/test-keys
```

Android 本体の `ro.build.fingerprint` と `ro.system.build.fingerprint`、TWRP 自身のプロパティは異なるので区別してください。

## 移行前の機能と維持する要件

CJK と欧文の install / restore ZIP を別々に生成しています。CJK は Noto Sans CJK / Serif CJK の可変 TTC、欧文は公式公開版 Google Sans Flex です。欧文は `sans-serif` と `sans-serif-condensed` のみを置き換え、元の Roboto / RobotoCondensed 20ファイルを補助フォールバック用に残します。serif / monospace の独立置換は将来用の担当範囲だけ予約しており、今回追加する必要はありません。

各パッチは担当部分だけを変更・復元し、他のパッチの状態を維持する必要があります。XML 全体の古いスナップショットを戻すと、後から導入した別パッチまで取り除いてしまうため、この方式は採用しません。

移行後も、共通構造・対象スロット・フォントのハッシュ照合、未知の変更の拒否、コメント・改行を含む担当外のバイト列の保持を維持します。再適用、書き込み順序、属性設定も維持する要件です。担当外スロットの内容の正当性は、そのスロットを担当するパッチが検証します。

既存のホスト実装 `scripts/font_slots.py` は、標準ライブラリの `hashlib` / `re` だけを使っています。この処理をホストと端末で共有し、二重実装を減らします。ビルド用の fontTools や ext4 解析は、端末側には不要です。

TWRP の ARM32 環境で実行できるランタイムが必要です。Python バイナリだけでなく標準ライブラリ・共有ライブラリ・起動時の依存関係も検討対象になります。一般の Android アプリ向け Python 配布物が、そのまま TWRP で動くことは未確認です。出典・バージョン・ハッシュ・ライセンスと再現可能な取得方法を明確にしたいです。System に Python を恒久インストールする必要はありません。

前セッションでは「まず TWRP の一時領域で同梱 Python が動くことを確かめ、XML 処理を共有する。mount や SELinux 設定は必要に応じて Edify に残せる」という方向性を話しました。これは候補であり、実装の分担を固定する指示ではありません。

## shell / awk の実装差による不具合と暫定修正

ホストで動作したリカバリー処理は、実機では次の環境差によって停止しました。

1. Toybox に awk がなく、BusyBox もありません。ただし独立した `/sbin/awk` はあり、バージョンは `20121220` でした。
2. TWRP の `/sbin/sh` では `hash` が `alias -t` のエイリアスになっており、同名のハッシュ計算関数が呼ばれず停止しました。
3. 単独 awk は `if (!active)` という式で `illegal arithmetic operator 345` を出しました。最小の再現例で確認済みです。`active == ""` ならその例は動きました。

`recovery/compose-fonts.sh` には単独 awk 対応と関数名の `file_hash` への変更を入れています。`recovery/split-fonts.awk` の単項否定を比較式に変更する編集も作業ツリーにあります。ただし **最後の awk 編集は再ビルド・実機検証を完了していません**。途中で Python 化の議論へ移りました。これらは Python 化前の暫定修正であり、残す必要があるかは設計に応じて判断してください。

単独 awk 対応時点の全26ホストテスト、後から追加した `hash` エイリアスの回帰テスト、最後にビルドした4 ZIP の統合テスト2件は成功しています。最新作業ツリー全体のテストを最後に一括実行した、という意味ではありません。

## 引き継ぎ時点の実機状態とバックアップ

開始時は Android が正常起動しており、CJK のみ適用した状態（C）でした。元 Roboto 20ファイルのハッシュも一致していました。System の空きは約1.7 GiBありました。

検証開始状態（S）へ戻すための CJK restore を2回試しましたが、いずれも上記の問題により **XML 合成準備で停止**しました。XML・フォントの書き換えに進んでいません。最後に確認した XML は次の C 状態のハッシュです。

```text
d511966983c2fc98dfbb5633b336b74709806c4a7315efa10616e78536ce8e55
```

最後の端末状態は TWRP 起動中で、インストーラー用の `/tmp/jp-font-system` はアンマウント済みです。接続・起動モード・ファイル状態は次セッション開始時に改めて確認してください。8遷移の検証、再適用試験、今回のパッチによる Android 起動・描画確認は未実施です。

System バックアップは **端末内に一旦保存せず PC へ直接取得**し、整合性検証に成功しています。

- 保存先: `build/device-validation/20260918-020133/system-before.ab`
- サイズ: `548407296` bytes
- SHA-256: `e653498603c3c17f1535008974a76053953d2f4edae93c1b646d61cbe24f110a`
- System 1エントリーの TWRP v3 バックアップ。C 状態のバックアップであり、元 ROM 状態のものではありません。
- 制御ブロック CRC・ペイロード MD5・gzip CRC・終端などを検証済み。バックアップからの復元実行は今回未実施です。

取得・検証・復元方法は `docs/twrp-pc-backup.md` にまとめ、以前の検証スクリプトを `scripts/verify_adb_backup.py` に戻しました。初回の System 読み取り専用確認画面が開いたままだと、バックアップ用 FIFO が未作成で0バイト終了しました。ユーザーの指摘どおり `twrp remountrw` で画面が閉じ、直接バックアップが可能になりました。

当時の検証では、ユーザーは TWRP の操作・バックアップ・ZIP 適用を ADB 経由で行うことを許可していました。バックアップは PC へ直接保存し、検証用 APK は追加せず、既存 UI で確認する方針でした。既存 UI だけではネイティブ描画の36設定や全フォールバックの選択を網羅できないため、未確認の範囲を報告で区別する必要がありました。

## 資料と成果物の注意点

- `README.md`、`docs/patch-format.md`: 機能・独立パッチの契約。
- `scripts/`、`recovery/`、`tests/`: 既存の実装とテスト。
- `docs/device-test-handoff.md`: Python 化前に作った実機検証の観点・期待状態・対象 ROM の詳細。そこに載っている ZIP ハッシュは古いものです。
- `docs/twrp-pc-backup.md`: 今回追加した PC 直接バックアップの記録と方法。
- `build/device-validation/20260918-020133/`: 初期状態、バックアップ、ログ、失敗のトレースと最小再現例、使用 ZIP ハッシュなどの証跡。Git 管理対象外です。
- 上記配下の `00-baseline/` と `00-baseline-retry/`: 2回の失敗記録。それぞれ使用した ZIP ハッシュを保存しています。
- `dist/` の現行 ZIP は Python 化前のもので、2回目の失敗時点のビルドです。最後の awk 編集もまだ含まれません。成功済みの実機成果物として扱わないでください。
- `build/` 配下の実機操作用 Python スクリプトはこのセッション専用の補助資料です。検証の全工程が完了したことを示すものではなく、新方式の設計を拘束しません。

Python 化後は、ランタイムの実機動作、独立導入・復元の契約、生成 ZIP の検証と実機確認がどこまでできたかを明確にしてください。既存のバックアップと失敗証跡を保持し、未確認の描画や復元を推測で合格扱いにしないでください。
